## Descripción general

Este proyecto es una **aplicación móvil multiplataforma** desarrollada con **React Native** y **Expo**.  
El objetivo principal es implementar un sistema CRUD (Crear, Leer, Actualizar, Eliminar) para gestionar una base de datos de **películas**, incorporando además un **sistema de autenticación de usuarios**.

El almacenamiento se realiza de manera local utilizando **SQLite**, cumpliendo con los requisitos de persistencia de datos en dispositivos móviles.

## 🚀 Características principales

- Autenticación de usuarios (inicio de sesión, registro, recuperación de contraseña).
- CRUD completo de películas (crear, leer, actualizar y eliminar).
- Base de datos local SQLite.
- Gestión de roles
- Interfaz adaptada a dispositivos móviles.
- Separación lógica por pantallas y servicios.

## Tecnologías utilizadas

| **React Native (Expo)** | Framework para crear apps móviles con JavaScript. |
| **SQLite** | Base de datos local integrada en la app. |
| **JavaScript (ES6)** | Lenguaje principal. |
| **Expo Go** | Aplicación para ejecutar el proyecto en dispositivos móviles. |

### Requisitos previos

- Tener instalado **Node.js** (versión recomendada: ≥ 16)
- Instalar la **CLI de Expo**:
  npm install -g expo-cli
-Instalar las dependencias:
  npm install
-Ejecutar el proyecto:
  npx expo start
